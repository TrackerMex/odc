FROM node:24-alpine

WORKDIR /app

RUN corepack enable && corepack prepare pnpm@10 --activate

COPY package.json pnpm-lock.yaml ./
RUN pnpm install

COPY . .

ARG PORT=3001
ENV PORT=${PORT}
EXPOSE ${PORT}

CMD ["pnpm", "start:dev"]
