# ODC — Status

**Última actualización**: 2026-07-18
**Features completadas**: 0/0 (`feature_list.json`)
**Pendientes**: definir stack y primeras features
**En producción**: no

---

## Qué es este proyecto

<!-- Descripción breve en 2-3 líneas: qué hace, para quién, por qué existe. -->

---

## Cómo arrancar

```bash
./init.sh
```

<!-- Añade aquí cualquier paso manual adicional (levantar servicios, cargar
     secretos, etc.) que init.sh no cubra. -->

---

## Estado actual

<!-- Qué funciona hoy, qué está a medias, qué decisiones quedaron pendientes. -->

---

## Última sesión

**2026-07-18** — Harness SDD instalado desde `harness-sdd-template` (commit
`293655e` de la plantilla). `./init.sh` en verde (0/0 features). Se vació
`init.config.sh` (stack aún sin definir; solo `node` como herramienta requerida).

Próximos pasos:
1. Definir stack y rellenar `init.config.sh` (INSTALL/BUILD/TEST/LINT)
2. Rellenar `docs/conventions.md` y la descripción de este archivo
3. Añadir primeras features a `feature_list.json`
4. `git init` + añadir `.obsidian/` al `.gitignore`

---

## Stack

<!-- Backend, frontend, infraestructura, versión de por definir. -->
