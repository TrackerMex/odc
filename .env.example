# Copia este archivo a .env y rellena los valores
DATABASE_URL=postgres://odc:odc@localhost:5432/odc
JWT_SECRET=
PORT=3001
# Password for the 3 seed users (pnpm seed). Dev default: odc-dev-password
SEED_PASSWORD=
